/*
Copyright (c) 2026 The Hiller Lab at the Senckenberg Gessellschaft für Naturforschung
Distributed under the terms of the Apache License, Version 2.0.
*/


/*
~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~
    softmask — configuration

    Infrastructure configuration — do not edit for normal runs.
    Scientific parameters main options live in params.json.

    Usage:
      nextflow run main.nf -params-file params.json -profile apptainer,slurm

    This file contains:
      1. params {}          — output dir, resource ceilings, nf-core boilerplate
      2. Process resources  — compute tiers (cpus, memory, time) via withLabel
      3. Per-step config    — container, conda, publishDir via withName
      4. Profiles           — executor and environment selection
      5. Reporting          — timeline, trace, DAG
~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~
*/

nextflow.enable.dsl = 2

cleanup = false
workDir = 'work'

manifest {
    name            = 'softmask'
    author          = 'Alejandro Gonzales-Irribarren, Michael Hiller'
    homePage        = 'https://github.com/hillerlab/softmask'
    description     = 'Lightweight workflow for soft-masking genomes'
    mainScript      = 'main.nf'
    nextflowVersion = '!>=25.04.6'
    version         = '0.0.1'
}

validation {
    help {
        enabled = true
        command = "nextflow run main.nf -profile <docker/singularity/.../institute>"
        fullParameter = "help_full"
        showHiddenParameter = "show_hidden"
    }
}

// ══════════════════════════════════════════════════════════════════════════════
// SECTION 1 — Infrastructure parameters
// Edit this section only to change compute limits or output behaviour.
// Scientific parameters live in params.json.
// ══════════════════════════════════════════════════════════════════════════════

params {

    // ── Output ──────────────────────────────────────────────────────────────
    outdir           = './results'
    publish_dir_mode = 'copy'

    // ── nf-core boilerplate ──────────────────────────────────────────────────
    validate_params  = true
    help             = false
    version          = false
    monochrome_logs  = false
    email            = null
    email_on_fail    = null
}

// ══════════════════════════════════════════════════════════════════════════════
// SECTION 2 — Compute resource tiers  (withLabel)
// ══════════════════════════════════════════════════════════════════════════════

process {
    shell    = ['/bin/bash', '-euo', 'pipefail']

    // Conda is disabled by default — use -profile apptainer (or conda to re-enable)
    conda.enabled = false

    // Retry up to maxRetries times, then terminate the workflow. Using a
    // dynamic errorStrategy (rather than maxErrors = 1) so a permanent task
    // failure ALWAYS aborts — `maxErrors = -1` previously masked silent data
    // loss.
    errorStrategy = { task.exitStatus in ((130..145) + 104 + 175) ? 'retry' : 'finish' }
    maxRetries    = 8

    // Short-lived steps
    withLabel: 'process_fast' {
        cpus   = 1
        memory = { 24.GB * task.attempt }
        time   = { 0.5.h * task.attempt }
    }
    // Medium-lived steps
    withLabel: 'process_medium' {
        cpus   = 1
        memory = { 50.GB * task.attempt }
        time   = { 2.h   * task.attempt }
    }

    // GPU
    withLabel: process_gpu {
        ext.use_gpu = { workflow.profile.contains('gpu') }
        accelerator = { workflow.profile.contains('gpu') ? 1 : null }
    }

// ══════════════════════════════════════════════════════════════════════════════
// SECTION 3 — Per-step configuration  (withName)
// Each block sets resources (if not using a label), container image, conda env,
// and whether outputs are published to results/.
// ══════════════════════════════════════════════════════════════════════════════

    // ── STEP 0: Genome preparation ──────────────────────────────────────────

    withName: '.*:TWOBIT_TO_FA' {
        cpus      = 1
        memory    = { 16.GB * task.attempt }
        time      = { 0.5.h * task.attempt }
        
        publishDir = [
            path: { "${params.outdir}/00_genome_prep" },
            mode: params.publish_dir_mode,
            pattern: "*.fasta"
        ]
    }

    withName: '.*:REFORMAT_GENOME_HEADERS' {
        label     = 'process_fast'
        
        publishDir = [
            path: { "${params.outdir}/01_renamed" },
            mode: params.publish_dir_mode,
            pattern: "*.fasta"
        ]
    }

    // ── STEP 1: Model repeats ───────────────────────────────────────────────────

    withName: '.*:REPEATMODELER_BUILDDATABASE' {
        label     = 'process_fast'
        conda     = "${projectDir}/environment.yml"
        publishDir = [
            path: { "${params.outdir}/02_database" },
            mode: params.publish_dir_mode,
            pattern: "*"
        ]
    }

    withName: '.*:REPEATMODELER_MODEL' {
        label     = 'process_fast'
        cpus      = 16

        ext.args   = {
            if (params.include_ltr) {
                return [
                    "-LTRStruct",
                ].join(' ')
            }
        }

        publishDir = [
            path: { "${params.outdir}/03_model" },
            mode: params.publish_dir_mode,
            pattern: "*"
        ]
    }

    // ── STEP 2: Mask repeats ─────────────────────────────────────────────

    withName: '.*:REPEATMASKER' {
        label        = 'process_fast'
        //array      = 500
        cpus         = 16

        ext.args   = {
            if (params.engine) {
                return [
                    "-engine ${params.engine}",
                ].join(' ')
            }
        }

        publishDir   = [
            path: { "${params.outdir}/04_mask" },
            mode: 'symlink',
            pattern: '*'
        ]
    }

    // ── STEP 3: Concatenate masked chunks ────────────────────────────────

    withName: '.*:PUBLISH_FASTA' {
        label        = 'process_fast'
        cpus         = 1
        publishDir   = [
            path: { "${params.outdir}/05_final" },
            mode: params.publish_dir_mode,
            pattern: '*.fasta'
        ]
    }

    withName: '.*:GZIP_FASTA' {
        label        = 'process_fast'
        cpus         = 1
        publishDir   = [
            path: { "${params.outdir}/05_final" },
            mode: params.publish_dir_mode,
            pattern: '*.fasta.gz'
        ]
    }

    withName: '.*:FA_TO_TWO_BIT' {
        label        = 'process_fast'
        cpus         = 1
        publishDir   = [
            path: { "${params.outdir}/05_final" },
            mode: params.publish_dir_mode,
            pattern: '*.2bit'
        ]
    }

    withName: '.*:MERGE_REPEATMASKER_REPEATS' {
        label        = 'process_fast'
        cpus         = 1
        publishDir   = [
            path: { "${params.outdir}/05_final" },
            mode: params.publish_dir_mode,
            pattern: '*.tsv'
        ]
    }
}

// ══════════════════════════════════════════════════════════════════════════════
// SECTION 4 — Profiles
// Combine an executor profile with an environment profile, e.g.:
//   -profile apptainer,slurm
// ══════════════════════════════════════════════════════════════════════════════

profiles {

    local {
        process.executor = 'local'
    }

    // ── SLURM (your HPC cluster) ────────────────────────────────────────────
    // Partition is selected automatically based on requested wall time:
    //   <= 24 h → batch
    //   >  24 h → long
    slurm {
        process.executor       = 'slurm'
        process.queue          = { task.time <= 24.h ? 'batch' : 'long' }
        process.clusterOptions = '--qos=public --signal=USR2@180 --export=ALL,SLURM_SKIP_EPILOG=1'
        process.stageInMode    = 'symlink'
        executor {
            queueSize         = 10000
            killBatchSize     = 1000000
            pollInterval      = '10 s'      // internal task-monitor tick (no SLURM RPC)
            queueStatInterval = '1 min'     // squeue cadence — halves RPC pressure
            submitRateLimit   = '100/1min'
            exitReadTimeout   = '5 min'    // tolerate slow .exitcode propagation; lets retry fire on ghosts
        }
    }

    debug {
        dumpHashes              = true
        process.beforeScript    = 'echo $HOSTNAME'
        cleanup                 = false
        nextflow.enable.configProcessNamesValidation = true
    }

    conda {
        conda.enabled           = true
        docker.enabled          = false
        singularity.enabled     = false
        podman.enabled          = false
        shifter.enabled         = false
        charliecloud.enabled    = false
        conda.channels          = ['conda-forge', 'bioconda']
        apptainer.enabled       = false
    }

    mamba {
        conda.enabled           = true
        conda.useMamba          = true
        conda.channels          = ['conda-forge', 'bioconda']
        docker.enabled          = false
        singularity.enabled     = false
        podman.enabled          = false
        shifter.enabled         = false
        charliecloud.enabled    = false
        apptainer.enabled       = false
    }

    docker {
        docker.enabled          = true
        conda.enabled           = false
        singularity.enabled     = false
        podman.enabled          = false
        shifter.enabled         = false
        charliecloud.enabled    = false
        apptainer.enabled       = false
        docker.runOptions       = '-u $(id -u):$(id -g)'
    }

    arm {
        docker.runOptions       = '-u $(id -u):$(id -g) --platform=linux/amd64'
        includeConfig 'conf/arm.config'
    }

    singularity {
        singularity.enabled     = true
        singularity.autoMounts  = true
        conda.enabled           = false
        docker.enabled          = false
        podman.enabled          = false
        shifter.enabled         = false
        charliecloud.enabled    = false
        apptainer.enabled       = false
    }

    podman {
        podman.enabled          = true
        conda.enabled           = false
        docker.enabled          = false
        singularity.enabled     = false
        shifter.enabled         = false
        charliecloud.enabled    = false
        apptainer.enabled       = false
    }

    shifter {
        shifter.enabled         = true
        conda.enabled           = false
        docker.enabled          = false
        singularity.enabled     = false
        podman.enabled          = false
        charliecloud.enabled    = false
        apptainer.enabled       = false
    }

    charliecloud {
        charliecloud.enabled    = true
        conda.enabled           = false
        docker.enabled          = false
        singularity.enabled     = false
        podman.enabled          = false
        shifter.enabled         = false
        apptainer.enabled       = false
    }

    apptainer {
        apptainer.enabled       = true
        apptainer.autoMounts    = true
        conda.enabled           = false
        docker.enabled          = false
        singularity.enabled     = false
        podman.enabled          = false
        shifter.enabled         = false
        charliecloud.enabled    = false
        apptainer.pullTimeout   = '60 min'
    }

    wave {
        apptainer.ociAutoPull   = true
        singularity.ociAutoPull = true
        wave.enabled            = true
        wave.freeze             = true
        wave.strategy           = 'conda,container'
    }

    gitpod {
        executor.name           = 'local'
        executor.cpus           = 4
        executor.memory         = 8.GB
        process {
            resourceLimits = [
                memory: 8.GB,
                cpus  : 4,
                time  : 1.h
            ]
        }
    }

    gpu {
        docker.runOptions       = '-u $(id -u):$(id -g) --gpus all'
        apptainer.runOptions    = '--nv'
        singularity.runOptions  = '--nv'
    }

    test {
        params.assembly_prefix  = 'test_assembly'
        params.genome           = "${projectDir}/assets/test_data/test_genome.fa"

        params.repeat_library   = ""
        params.include_ltr      = true
        params.engine           = "ncbi" // [ options: 'ncbi', 'crossmatch', 'hmmer' ]
        params.chunks           = 1000   // 1kb

        params.output_format    = '2bit' // [ options: 'fasta', '2bit', 'gz' ]
        params.outdir           = 'test_results'
        params.use_container    = false
     }
}

// ══════════════════════════════════════════════════════════════════════════════
// SECTION 5 — Container image
// Set the container image to use for all processes through use_container option.
// If not set, the default is to use the apptainer cache image for every module.
// To modify go to params.json and set 'use_container' to false.
// ══════════════════════════════════════════════════════════════════════════════

if (params.use_container) {
  process {
    withName: '.*' {
        container = System.getenv('NXF_CONTAINER_IMAGE') ?:
            'ghcr.io/hillerlab/softmask:latest'
    }
  }
}

// ══════════════════════════════════════════════════════════════════════════════
// SECTION 6 — Execution reporting
// ══════════════════════════════════════════════════════════════════════════════

def trace_timestamp = new java.util.Date().format('yyyy-MM-dd_HH-mm-ss')

timeline {
    enabled = true
    file    = "${params.outdir}/pipeline_info/execution_timeline_${trace_timestamp}.html"
}

report {
    enabled = true
    file    = "${params.outdir}/pipeline_info/execution_report_${trace_timestamp}.html"
}

trace {
    enabled = true
    file    = "${params.outdir}/pipeline_info/execution_trace_${trace_timestamp}.txt"
}

dag {
    enabled = true
    file    = "${params.outdir}/pipeline_info/pipeline_dag_${trace_timestamp}.html"
}

// ══════════════════════════════════════════════════════════════════════════════
// SECTION 7 — Environment configuration
// ══════════════════════════════════════════════════════════════════════════════

/*
~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~
    ENVIRONMENT CONFIG
~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~
*/

// Export these variables to prevent local Python/R libraries from conflicting with those in the container
// The JULIA depot path has been adjusted to a fixed path `/usr/local/share/julia` that needs to be used for packages in the container.
// See https://apeltzer.github.io/post/03-julia-lang-nextflow/ for details on that. Once we have a common agreement on where to keep Julia packages, this is adjustable.

env {
    PYTHONNOUSERSITE = 1
    R_PROFILE_USER   = "/.Rprofile"
    R_ENVIRON_USER   = "/.Renviron"
    JULIA_DEPOT_PATH = "/usr/local/share/julia"
}

/*
~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~
    REGISTRY CONFIG
~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~
*/

// Set default registry for Apptainer, Docker, Podman, Charliecloud and Singularity independent of -profile
// Will not be used unless Apptainer / Docker / Podman / Charliecloud / Singularity are enabled
// Set to your registry if you have a mirror of containers
apptainer.registry    = 'quay.io'
docker.registry       = 'quay.io'
podman.registry       = 'quay.io'
singularity.registry  = 'quay.io'
charliecloud.registry = 'quay.io'
